name: Weekly ToC Digest

on:
  schedule:
    # Mondays 08:00 America/Los_Angeles ≈ 16:00 UTC (adjust if you like)
    - cron: "00 16 * * 1"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  digest:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install --upgrade openai httpx certifi

      - name: Network check (OpenAI)
        run: |
          python - << 'PY'
          import socket
          host = "api.openai.com"
          print("Resolving:", host)
          print(socket.gethostbyname(host))
          print("OK: DNS resolve")
          PY
          curl -I https://api.openai.com/v1/models --max-time 20

      - name: Run digest
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          HTTP_PROXY: ""
          HTTPS_PROXY: ""
          ALL_PROXY: ""
          NO_PROXY: "api.openai.com"
          OPENAI_MODEL_CHEAP: "gpt-5-nano"
          OPENAI_MODEL_FINAL: "gpt-5.2"
          MIN_SCORE_READ: "0.35"
          LOOKBACK_DAYS: "7"
          SUMMARY_MAX_CHARS: "500"
          PREFILTER_KEEP_TOP: "200"
          BATCH_SIZE: "50"
          REFINE_TOP_N: "40"
          REFINE_BATCH_SIZE: "20"
          OUTPUT_DIR: "output"
          HTML_OUTPUT_DIR: "html_outputs"
          CATEGORY_MAX_ITEMS: "5"
        run: python digest.py

      - name: Commit digest artifacts
        run: |
          git config user.name "toc-digest-bot"
          git config user.email "toc-digest-bot@users.noreply.github.com"
          git add digest.md latest.md output html_outputs
          git commit -m "Update weekly ToC digest" || exit 0
          git push
